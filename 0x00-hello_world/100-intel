#!/bin/bash

# Get the C file name from the environment variable
cfile=$CFILE

# Create the output file name with the .s extension
sfile="${cfile%.*}.s"

# Generate the assembly code using GCC
gcc -S -masm=intel "$cfile" -o "$sfile"

# Print the assembly code file name
echo "Generated assembly code: $sfile"

